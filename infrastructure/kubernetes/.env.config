# ============================================
# STREAMIA - Variables NO sensibles
# ============================================
# Estas variables se cargan como ConfigMap
# Todos los servicios las reciben automáticamente

NODE_ENV=production
CORS_ORIGIN=http://localhost:5173,https://streamia-client2.vercel.app

# JWT (tiempos de expiración - no sensibles)
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Infrastructure URLs (dentro del cluster)
REDIS_URL=redis://redis:6379
RABBITMQ_URL=amqp://streamia:streamia@rabbitmq:5672

# Email Config (no sensibles)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
EMAIL_FROM=streamia8@gmail.com

# Cloudinary (solo el nombre del cloud)
CLOUDINARY_CLOUD_NAME=dwmt0zy4j
